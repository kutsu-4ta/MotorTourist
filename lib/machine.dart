import 'package:flutter/material.dart';

class Machine extends StatelessWidget {
  String machineName = 'スーパーカブ';

  @override
  Widget build(BuildContext context) {
    return Text(this.machineName);
  }
}
